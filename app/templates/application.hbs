<div class="container">
  <div class="navbar">
    <div class="navbar-inner">
      <a class="brand" href="#">Mashboard</a>
      <ul class="nav pull-right">
        <li>
          {{#linkTo "my_mashboard"}}
            My Mashboard
            <span class="badge">
              {{App.savedSessions.sessions.length}}
            </span>
          {{/linkTo}}
        </li>
      </ul>
    </div>
  </div>

  {{outlet}}

  <footer>
  </footer>
</div>
